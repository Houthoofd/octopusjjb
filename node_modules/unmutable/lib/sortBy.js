'use strict';

Object.defineProperty(exports, "__esModule", {
    value: true
});

var _prep = require('./internal/prep');

var _prep2 = _interopRequireDefault(_prep);

function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }

function _toConsumableArray(arr) { if (Array.isArray(arr)) { for (var i = 0, arr2 = Array(arr.length); i < arr.length; i++) { arr2[i] = arr[i]; } return arr2; } else { return Array.from(arr); } }

var defaultComparator = function defaultComparator(a, b) {
    if (a < b) {
        return -1;
    }
    if (a > b) {
        return 1;
    }
    return 0;
};

exports.default = (0, _prep2.default)({
    name: 'sortBy',
    immutable: 'sortBy',
    array: function array(comparatorValueMapper) {
        var comparator = arguments.length > 1 && arguments[1] !== undefined ? arguments[1] : defaultComparator;
        return function (value) {
            return [].concat(_toConsumableArray(value)).sort(function (a, b) {
                return comparator(comparatorValueMapper(a), comparatorValueMapper(b));
            });
        };
    }
});